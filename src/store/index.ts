/**
 * Global state management entry point.
 *
 * Recommended options (install one):
 *
 * Option A — Zustand (lightweight, simple):
 *   npx expo install zustand
 *
 * Option B — Redux Toolkit (robust, scalable):
 *   npx expo install @reduxjs/toolkit react-redux
 *
 * After installing, configure your stores in src/store/slices/
 * and export them from here.
 */
